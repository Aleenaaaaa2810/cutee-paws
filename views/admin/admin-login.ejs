<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>

    <title>Admin Login</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body, html {
            height: 100%;
            font-family: 'Poppins', sans-serif;
            background-color: #1a1a1a; /* Black background */
            display: flex;
            justify-content: center;
            align-items: center;
            color: white;
        }

        .login-container {
            position: relative;
            background: rgba(0, 0, 0, 0.8); /* Darker transparent overlay */
            padding: 40px;
            border-radius: 15px;
            width: 380px;
            box-shadow: 0 15px 40px rgba(0, 0, 0, 0.6);
            overflow: hidden;
            transition: transform 0.4s ease-in-out, box-shadow 0.4s ease-in-out;
        }

        .login-container:hover {
            transform: translateY(-10px);
            box-shadow: 0 30px 60px rgba(0, 0, 0, 0.7);
        }

        h2 {
            text-align: center;
            font-size: 28px;
            margin-bottom: 20px;
            font-weight: 600;
            color: #f1c40f; /* Yellow color */
        }

        .form-group {
            margin-bottom: 20px;
        }

        label {
            font-size: 16px;
            font-weight: bold;
            color: #f1c40f; /* Yellow for labels */
            margin-bottom: 5px;
            display: block;
        }

        input[type="text"], input[type="password"] {
            width: 100%;
            padding: 15px;
            font-size: 16px;
            border-radius: 10px;
            border: 1px solid #f1c40f; /* Yellow border */
            background: #333; /* Dark gray background for inputs */
            color: white;
            margin-bottom: 15px;
            transition: all 0.3s ease;
        }

        input[type="text"]:focus, input[type="password"]:focus {
            background: #444; /* Slightly lighter gray on focus */
            box-shadow: 0 0 5px 2px rgba(241, 196, 15, 0.8); /* Yellow focus */
            outline: none;
        }

        input[type="submit"] {
            width: 100%;
            padding: 15px;
            background-color: #f1c40f; /* Yellow button */
            color: white;
            border: none;
            border-radius: 10px;
            font-size: 18px;
            cursor: pointer;
            transition: background-color 0.3s ease, transform 0.3s ease;
        }

        input[type="submit"]:hover {
            background-color: #f39c12; /* Darker yellow on hover */
            transform: scale(1.05);
        }

        .forgot-password {
            text-align: center;
            margin-top: 15px;
        }

        .forgot-password a {
            color: #f1c40f; /* Yellow for link */
            font-size: 14px;
            text-decoration: none;
            transition: color 0.3s ease;
        }

        .forgot-password a:hover {
            color: #f39c12; /* Darker yellow for hover effect */
            text-decoration: underline;
        }

        .logo {
            display: block;
            width: 120px;
            margin: 0 auto 20px;
            animation: bounce 1s ease infinite;
        }

        @keyframes bounce {
            0%, 20%, 50%, 80%, 100% {
                transform: translateY(0);
            }
            40% {
                transform: translateY(-10px);
            }
            60% {
                transform: translateY(-5px);
            }
        }

    </style>
</head>
<body>

    <div class="login-container">
        <!-- Logo with animation -->
        <img src="https://www.example.com/logo.png" alt="" class="logo">
        
        <h2>Admin Login</h2>
        
        <form action="/admin/login" method="POST">
            <div class="form-group">
                <label for="email">Email</label>
                <input type="text" id="email" name="email" required placeholder="Enter Email">
            </div>
            <div class="form-group">
                <label for="password">Password</label>
                <input type="password" id="password" name="password" required placeholder="Enter Password">
            </div>
            <input type="submit" value="Login">
        </form>
        
        <% if (message) { %>
            <script>
              Swal.fire({
                icon: 'error',
                title: 'Oops...',
                text: '<%= message %>',
              });
            </script>
            <% } %>


        
        <!-- <div class="forgot-password">
            <a href="#">Forgot Password?</a>
        </div> -->
    </div>

</body>
</html>
